module.exports.validateInput = async(required_attributes, input) => {
    for (let attribute in required_attributes){
        console.log("Going through for loop... -> ", attribute)
        console.log(input.hasOwnProperty(attribute))
        if(!input.hasOwnProperty(attribute)){
            throw new Error('Invalid input..')
        }
    }
}